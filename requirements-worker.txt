# Зависимости воркера автозагрузки (scripts/ingest_worker.py).
# Streamlit и библиотеки отчетов не нужны: воркер работает без интерфейса.
#
# Верхние границы обязаны совпадать с requirements.txt: воркер и интерфейс
# выполняют один и тот же код обработки выгрузок (services/ingest.py), и
# разойтись по мажорной версии pandas им нельзя — иначе один и тот же файл
# обработается по-разному в зависимости от того, загрузили его руками или
# он приехал автозагрузкой.
numpy>=1.24,<3.0
openpyxl>=3.1,<4.0
pandas>=2.0,<4.0
pyarrow>=14.0,<26.0
scikit-learn>=1.3,<2.0
supabase>=2.0,<3.0
