altair>=5.0
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
pyarrow>=14.0
python-docx>=1.1
reportlab>=4.0
requests>=2.31
scikit-learn>=1.3
# Нижняя граница: интерфейс использует width="stretch" вместо снятого с
# поддержки use_container_width. Верхняя: Streamlit Cloud ставит самую свежую
# версию при каждом передеплое, и мажорный выпуск не должен прилетать в прод
# незаметно — поднимать осознанно, прогнав tests/test_ui_smoke.py.
streamlit>=1.63,<2.0
supabase>=2.0

matplotlib>=3.8
